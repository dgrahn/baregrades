<%= form_for([@course, @assignment_type]) do |f| %>
	<%= f.label :name %><br />
	<%= f.text_field :name %>

	<%= f.label :description %><br />
	<%= f.text_area :description %>

	<%= f.label :worth %><br />
	<%= f.text_field :worth %>

	<%= f.submit %>
<% end %>
