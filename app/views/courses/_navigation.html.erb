<%= link_to "Summary", course_path(@course), class:cp(course_path(@course)) %>
<%= link_to "Calendar", course_calendar_path(@course), class:cp(course_calendar_path(@course)) %>
<%= link_to "Info", course_info_path(@course), class:cp(course_info_path(@course)) %>
<%= link_to "Analysis", course_analysis_path(@course), class:cp(course_analysis_path(@course)) %>